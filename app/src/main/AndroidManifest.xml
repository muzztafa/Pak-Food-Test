<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pakfoodproductstest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Reports"></activity>
        <activity android:name=".DriverView" />
        <activity android:name=".SignInActivity_Driver" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".InvoiceDetailsActivity" />
        <activity android:name=".NewInvoiceActivity" />
        <activity android:name=".SignInActivity_Admin" />
        <activity android:name=".CustView" />
        <activity android:name=".PhoneAuth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>